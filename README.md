# OsteoJump

Статический лендинг с поддержкой мультиязычности и адаптивной версткой.

## Особенности

- 🌍 Поддержка 5 языков: русский, английский, немецкий, польский, украинский
- 📱 Полностью адаптивная верстка для всех устройств
- 🚀 Автоматический деплой на GitHub Pages при коммите в main ветку
- ⚡ Быстрая загрузка без зависимостей
- 🎨 Современный дизайн с плавными переходами

## Технологии

- HTML5
- CSS3 (Grid, Flexbox, Media Queries)
- Vanilla JavaScript
- GitHub Pages для хостинга
- GitHub Actions для CI/CD

## Структура проекта

```
osteojump/
├── index.html          # Главная страница
├── css/
│   └── style.css      # Стили с адаптивной версткой
├── js/
│   ├── i18n.js        # Логика переключения языков
│   └── main.js        # Основная логика приложения
├── locales/           # JSON файлы с переводами
│   ├── ru.json
│   ├── en.json
│   ├── de.json
│   ├── pl.json
│   └── uk.json
└── .github/
    └── workflows/
        └── deploy.yml # GitHub Actions workflow
```

## Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/osteojump.git
cd osteojump
```

2. Запустите локальный сервер:
```bash
# Рекомендуемый способ - с поддержкой clean URLs
make server

# Или напрямую через Python скрипт
python3 server.py

# Или простой HTTP сервер (без поддержки clean URLs)
make server-simple
# или
python3 -m http.server 8000
```

3. Откройте в браузере: `http://localhost:8000`

### Дополнительные команды

```bash
# Показать все доступные команды
make help

# Создать превьюшки для дипломов (оптимизация изображений)
make thumbnails

# Удалить созданные превьюшки
make clean
```

## Деплой

Сайт автоматически деплоится на GitHub Pages при каждом коммите в ветку `main`.

Подробные инструкции по настройке деплоя и подключению кастомного домена см. в [DEPLOYMENT.md](DEPLOYMENT.md).

## Добавление нового языка

1. Создайте новый JSON файл в папке `locales/` (например, `fr.json`)
2. Скопируйте структуру из существующего файла и переведите содержимое
3. Добавьте кнопку переключения языка в `index.html`:
```html
<button class="lang-btn" data-lang="fr">FR</button>
```
4. Добавьте код языка в массив `supportedLangs` в `js/i18n.js`

## История изменений

### 2026-01-07 - Исправление Action Bar и iOS Safe Area

**Проблемы, которые были решены:**

1. **Action bar не прилипал к низу экрана**
   - Изменено позиционирование с `position: fixed` на `position: sticky`
   - Упрощен подход - убраны CSS переменные с жестко заданными высотами (`--action-bar-height`)
   - Убраны сложные вычисления padding для footer и main

2. **Некорректная обработка iOS Safe Area**
   - Проблема: использовался `calc(padding + env(safe-area-inset-bottom))` - суммировал padding и safe area
   - Решение: изменено на `max(padding, env(safe-area-inset-bottom))` - берет большее значение
   - Результат: правильный отступ над home indicator без избыточного белого пространства

3. **Попытки использования динамического viewport (dvh)**
   - Протестировано использование `100dvh` vs `100vh`
   - Оставлен `100dvh` как оптимальное решение для мобильных браузеров

**Технические детали:**

- Action bar теперь часть потока документа (`position: sticky`)
- Safe area обрабатывается только в `padding-bottom` action bar
- Desktop: `max(16px, 0px)` = 16px обычный padding
- iOS: `max(16px, ~34px)` = 34px отступ над home indicator
- Чистый CSS без хардкода и workarounds

**Измененные файлы:**
- `css/style.css` - упрощена структура action bar, исправлена обработка safe area

### 2026-01-06 - Исправление навигации и адаптивной верстки

**Проблемы, которые были решены:**

1. **Адаптивное меню навигации**
   - Реализована логика автоматического определения overflow элементов
   - Элементы меню, которые не помещаются, автоматически перемещаются в гамбургер-меню
   - Единая логика для всех размеров экрана (без разделения на mobile/desktop)

2. **Прыганье отступов при изменении размера окна**
   - Добавлен `overflow-y: scroll` к html для стабильной ширины viewport
   - Убран дублирующийся padding из навигации
   - Удалены изменения padding в медиа-запросах, вызывающие прыжки на breakpoints

3. **Overflow селектора языка**
   - Исправлена структура: навигация теперь использует padding только от `.container`
   - Добавлено `max-width: 100%` для предотвращения выхода за пределы

4. **Масштабирование страницы при резком изменении ширины**
   - Добавлен `overflow-x: hidden` к html и body
   - Добавлено `max-width: 100vw` к body для предотвращения зума

**Технические детали:**

- Гамбургер-кнопка всегда резервирует место в layout (даже когда скрыта)
- JavaScript рассчитывает доступное пространство и показывает/скрывает элементы
- Навигация имеет стабильную ширину на всех размерах экрана

**Измененные файлы:**
- `css/style.css` - исправления layout и overflow
- `js/main.js` - логика адаптивного меню

## Лицензия

© 2024 OsteoJump. Все права защищены.






